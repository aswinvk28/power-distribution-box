const ElementType = {
    OUTPUTS: 'Outputs',
    FRONTS: 'Front',
    ADDONS: 'Addons',
    INPUTS: 'Inputs'
};

const SVG_ELEMENTS = {
    FULL_WIDTH: '480px'
}

const Constants = {
    useLocalStorage: true,
    gridSize: 16,
    ElementType: ElementType,
    SVG_ELEMENTS: SVG_ELEMENTS
};

export default Constants;
