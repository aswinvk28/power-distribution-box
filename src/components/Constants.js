const ElementType = {
    OUTPUTS: 'Outputs',
    FRONTS: 'Front',
    ADDONS: 'Addons',
    INPUTS: 'Inputs'
};

const Constants = {
    useLocalStorage: true,
    gridSize: 16,
    ElementType: ElementType
};

export default Constants;
