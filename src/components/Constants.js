const ElementType = {
    OUTPUTS: 'Outputs',
    FRONTS: 'Front',
    ADDONS: 'Addons',
    INPUTS: 'Inputs'
};

const COLORS = {
    'BRAND_TITLE': 'b00110',
    
}

const SCALE = {
    FRONT_SIDE: 5,
    REAR_SIDE: 5
};

const SVG_ELEMENTS = {
    FULL_WIDTH: '480px'
};

const Constants = {
    useLocalStorage: true,
    gridSize: 16,
    ElementType: ElementType,
    SVG_ELEMENTS: SVG_ELEMENTS,
    SCALE: SCALE
};

export default Constants;
